BEGIN;
CREATE TABLE "portfolio_comment" ("id" integer NOT NULL PRIMARY KEY AUTOINCREMENT, "content" varchar(500) NOT NULL, "date" datetime NOT NULL, "parent" integer NULL, "image" varchar(200) NULL, "username" varchar(50) NULL);
COMMIT;